# Variables

We use variable to temporarily store data in computer's memory
</br>
<p>

We can receive input from the user by calling the input() function

birth_year = int(input('birth_year: '))

<strong>The input() functiona always reutrns data as a strign. So, we'are converting the result inot an integer by calling the built-in int() function.</strong>


</p>

# String

 <p>We can define string using single('') or double quote
 To define a multi-line string, we surround our string with triple quote """

 <br/>
 We can get individual characters ina string using square brackets [].
<br/>

```
course = 'Python for beginners'
course[0] # reutrn the first character
couuse[1]
```
</p>

# String method

<ul>
	.upper()
	.lower()
	.title() # to capitalize the first letter of every word
	.find() # returns the index of the first ocurrences of p ( or -1 if not found)
	.replace(base, change)
</ul>

<p> To check if a string contains a character ( or a sequence of characters), we use the in operator:

```
	[-1] # returns the first item from the end
	[-2] # reutnr the second item from the end
	[::-1] # reverse the string.
	contains = 'Python' in course
	+ 
	-
	*
	/ # returns a float
	// # return an int
	% # return the remainder of divsion
	** # return power
	.append() # add value to the end
	.insert() # add value at tindex position
	.remove() # remove value
	.pop()  # remove the last item
	.clear() # remove all 
	.index() # reutrn the index of first
	.sort() # sort the list
	.reverse() # reverse the list
	.copy() # returna copy of the list
```
 </p>
